# Generated knowledge graph, rebuilt and committed by CI.
graphify-out/

# Markdown belongs to docs-lint, which already gates every .md file in this
# repo. Prettier's markdown pass only rewrites emphasis markers and re-wraps
# prose, which is churn against a linter that owns the same files.
*.md


# Lockfiles are npm's format, not ours.
package-lock.json
**/package-lock.json

node_modules/
**/node_modules/
**/dist/
**/bin/

# Agent worktrees are full checkouts of this repo living inside it. The
# pre-commit hook runs `prettier --write .` with `pass_filenames: false`, so
# without these a commit here reformats files in a sibling branch's checkout.
.claude/
.worktrees/

# Generated by the test runners, and `prettier --check .` fails on the HTML
# report whether or not anyone asked for coverage.
**/coverage/
