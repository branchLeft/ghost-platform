{
  "extends": "./tsconfig.json",
  // The shared base sets `noEmit`, because most projects extending it only ever
  // type-check. This is the one config here that has to produce output.
  "compilerOptions": {
    "noEmit": false
  },
  "include": ["src/**/*.ts"],
  "exclude": ["test/**/*.ts"]
}
