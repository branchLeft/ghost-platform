{
  "name": "@branchleft/ghost-platform-tenant",
  "version": "0.4.0",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/branchLeft/ghost-platform.git",
    "directory": "infra/tenant"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "public"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "test:unit": "vitest --run"
  },
  "dependencies": {
    "@pulumi/gcp": "^9.34.1",
    "@pulumi/pulumi": "^3.259.0",
    "@pulumi/random": "^4.18.0"
  },
  "devDependencies": {
    "@branchleft/tsconfig": "^0.3.1",
    "@types/node": "^26",
    "typescript": "^5.9.0",
    "vitest": "^4.1.11"
  }
}
