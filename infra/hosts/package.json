{
  "name": "ghost-platform-hosts",
  "private": true,
  "main": "index.ts",
  "description": "Pulumi program for the estate's application and database hosts (app1, db1). See infra/README.md.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest --run"
  },
  "dependencies": {
    "@branchleft/hetzner-host": "0.1.0",
    "@pulumi/hcloud": "1.41.0",
    "@pulumi/pulumi": "^3.255.0"
  },
  "devDependencies": {
    "@branchleft/tsconfig": "^0.3.1",
    "@types/node": "^26",
    "typescript": "^5.9.0",
    "vitest": "^4.1.10"
  }
}
